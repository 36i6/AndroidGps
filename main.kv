#:import MapView kivy_garden.mapview.MapView

MapView:
    lat: 10
    lon: 10
    zoom: 10
    on_zoom:
        self.zoom = 10 if self.zoom < 10 else self.zoom
    MapMarkerPopup:
        lat: 10
        lon: 10
        source: 'marker.png'
        on_release:
            pass
        Button:
            on_release:
                pass
